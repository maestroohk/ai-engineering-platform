# M0.5 — Architecture Refinement Handoff

> **Session:** `m0.5-architecture-refinement`
> **Date:** 2026-07-10
> **Type:** architecture
> **Milestone:** M0.5 (between M1 and M2; not a
> product milestone)

---

## What was done

The M0.5 architecture refinement delivered the
ten improvements the brief required. The
deliverables, in delivery order, with file
paths:

1. **`VISION.md`** (root) — the permanent
   vision document. The destination. Ten
   sections. Tier 1 of the document hierarchy.
2. **`.ai/backlog/`** — `README.md`,
   `epics.md` (5 epics), `features.md` (14
   features), `capabilities.md` (21
   capabilities with dependencies), `ideas.md`
   (15 ideas: 10 rejected, 5 deferred).
3. **`.ai/state/decision-log.md`** — 12 small
   in-flight decisions (D-001 through D-012).
4. **`.ai/state/capability-mapping.md`** —
   four-layer model (0 Infrastructure, 1
   Provider Model, 2 Domain Orchestration,
   3 User Surface), 21 capabilities, five
   composition chains.
5. **`.ai/state/*.json` + `.ai/state/*.schema.json`**
   — six canonical state files (project,
   milestones, capabilities, features,
   providers, tasks) with JSON Schema
   draft-07 schemas. Two-layer model:
   JSON canonical, Markdown projection.
6. **`.ai/state/session.json` + `.ai/state/session.schema.json`**
   — the session self-awareness state.
   Captures the current session's id, type,
   scope, current understanding, last action,
   and intended next action.
7. **`docs/dashboard.md`** — the Product
   Dashboard Definition. The M2 landing page
   contract. 11 sections. 9 dashboard
   sections, 7 data-owning with the four
   state slots per ADR-014.
8. **`.ai/workflows/tool-dogfooding.md`** —
   added § 1.4 (the four-stage evolution
   path: External tool → Manual usage →
   Integrated provider → Native platform
   capability) and § 4 (per-tool dogfooding
   profile for Lavish Axi, Treehouse, No
   Mistakes, GNHF, Firstmate).
9. **`.ai/README.md`** — added § 10
   (Documentation Architecture). Nine
   tiers, seven anti-drift rules,
   validation pass results.
10. **`implementation-report-m0.5-architecture-refinement.md`**
    — the architecture review report.
    Architecture score: 23 → 42 (+19) on
    five dimensions.

## Files modified (consolidated)

- `AGENTS.md` — Document Map (tiered) added
  in § 6. 17 rules unchanged.
- `.ai/README.md` — § 10 added.
- `.ai/workflows/tool-dogfooding.md` —
  Evolution Path + Per-Tool Profiles added.
- `.ai/state/README.md` — Two-Layer Model
  section added.
- `.ai/state/current.md` — State Architecture
  section added.
- `.ai/state/task-board.md` — State
  Architecture section added.

## What was NOT done

- **No application code modified.** M0.5
  is documentation-only. The test count is
  unchanged at 80 passed / 4 skipped / 0
  failed.
- **No completed milestone (M0, M1)
  invalidated.** The M0 / M1 / M1.1 / M1.2
  evidence is unchanged.
- **No `AGENTS.md` rule changed.** The 17
  rules are unchanged; the Document Map
  addition in § 6 is metadata, not a rule.
- **No `ARCHITECTURE.md` boundary changed.**
- **No ADR added.** The 12 decision-log
  entries are pre-ADR.
- **No `ROADMAP.md` change.** The M0.5 row
  is the M0.5 milestone entry the brief
  itself produced; it is not modified.
- **No external tool invoked.** The
  `lavish-axi` M1 review remains
  `Blocked`.

## State at session end

- **Active milestone:** M0.5 (in
  progress; closeout is the
  coherent commit + handoff +
  state updates at the end of
  this session).
- **Last completed milestone:** M1 —
  Design System Core (2026-07-10).
- **Last stable commit (pre-M0.5):**
  `8fae9517d2c10dceb90c6b3475f1635d5f86a8bd`.
- **Build status:** passing (unchanged;
  no application code touched).
- **Test status:** 80 passed, 4
  skipped, 0 failed (unchanged).
- **Worktree state:** M0.5 is
  documentation-only; no worktree
  required.

## Architectural score

- **Clarity:** 5 → 9 (+4)
- **Separability:** 4 → 8 (+4)
- **Extensibility:** 5 → 8 (+3)
- **Learnability:** 6 → 9 (+3)
- **AI-Session-Readiness:** 3 → 8 (+5)
- **Total:** 23 → 42 (+19)

## Intended next action

> **M2.1 — Application Shell Skeleton.**
> Read
> [`.ai/plans/M2.1-application-shell-skeleton.md`](../plans/M2.1-application-shell-skeleton.md).
> The plan is `Awaiting Approval`; the
> first action is to either approve the
> plan and start M2.1 implementation,
> or amend the plan and re-submit it.

## What the next session should read

1. `AGENTS.md` — the constitution.
2. `.ai/session-start.md` — the
   operational sequence.
3. `PRODUCT.md` — the product definition.
4. `.ai/state/current.md` — the one-page
   snapshot (this session's reconciliation
   is recorded at the top).
5. `.ai/state/task-board.md` — the live
   work queue (M2.1 is `Ready`, plan
   `Awaiting Approval`).
6. `.ai/handoffs/latest.md` — this
   handoff (mirrored).
7. `.ai/state/session.json` — the
   self-awareness state for this session.
8. `implementation-report-m0.5-architecture-refinement.md`
   — the architecture review.

## Known limitations (carried forward)

- **No automated validation of JSON state
  files against their schemas.** A
  `validate-state.sh` script using `ajv`
  is a future deliverable.
- **No automated graph check (capability
  graph and backlog `depends_on` graph).**
  A `validate-graphs.sh` script is a
  future deliverable.
- **Per-tool profiles for GNHF and
  Firstmate are aspirational (Stage 1 by
  intent, not by evidence).** A milestone
  that exercises a tool will update the
  profile to the *verified* stage.
- **The dashboard's "first paint < 200ms"
  criterion is defined but not validated.**
  M2 implements the shell; M6 implements
  the live data; the validation is M2's
  or M6's.
- **The `lavish-axi` M1 review is still
  Blocked** (the tool is not installed on
  the host). The next attempt is the M2
  closeout's review gate.

## How to close M0.5

This session ends the M0.5 refinement.
The session end performs:

- The coherent commit (Rule 17) that
  includes the implementation, the
  documentation, the implementation
  report, the state updates, and this
  handoff. The commit is local; pushing
  requires explicit authorisation.
- The project-continuity state updates
  (Rule 15) — the `current.md` and
  `task-board.md` files are updated;
  this handoff is mirrored to
  `latest.md`.
- A short closing message confirming
  what was done, what was not, and what
  to read next.

## Linked artefacts

- `VISION.md` — the permanent vision
  document.
- `.ai/backlog/` — the engineering
  backlog.
- `.ai/state/` — the structured state,
  the decision log, the capability
  mapping, the self-awareness state.
- `docs/dashboard.md` — the dashboard
  definition.
- `.ai/workflows/tool-dogfooding.md` —
  the improved dogfooding workflow.
- `.ai/README.md` — the AI collaboration
  hub, with the documentation
  architecture map.
- `implementation-report-m0.5-architecture-refinement.md` —
  the architecture review.
- This handoff (the M0.5 handoff).
