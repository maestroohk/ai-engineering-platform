@namespace AiEng.Platform.App.Components.Shell

<AppShellRegion Name="sidebar">
    <AppStack Gap="AppStackGap.Small">
        <AppEmptyState>
            <Title>Sidebar lands in M2.2</Title>
            <Description>The navigation registry and the registry-driven sidebar arrive in M2.2 (Navigation Registry and Sidebar). The slot reserves the region so M2.2 has a stable target.</Description>
        </AppEmptyState>
    </AppStack>
</AppShellRegion>
